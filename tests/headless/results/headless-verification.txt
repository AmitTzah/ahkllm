# 2026-08-13T16:31:07.362Z | run of 21 scenario(s)
PASS | #56 | Stopping a stream before the first token is a clean cancel (static check) | _finalizeStreaming checks _streamCancelled before the empty-content branch, so pressing Stop before the first token is a clean cancellation (no API-key banner) | 0.0s
PASS | #98 | StreamHandler cancel branch cleans up _stream state (bug fixed) | StreamHandler _finalizeStreaming wasCancelled branch calls _handleStreamCancelled then _cleanupStreamState before return - no _stream* keys leak into the next request | 0.0s
PASS | #110 | Streaming deletes temp files on success and error (credential leak fixed) | StreamCompletion._handleStreamComplete and StreamError._handleStreamError now call deleteTempFiles (like the cancel path), so request/cURL files with the Bearer token never linger in %TEMP% | 0.0s
PASS | #133 | Cancelling a stream mid-response records a fake 0-token API request in chat_usage and inflates the thread cumulative input by the un-billed parent context (header vs dashboard disagree) | cancelled mid-stream: partial row={"role":"assistant","content":"Hello from the mock LLM. ","token_count":0,"prompt_tokens":0,"active_path_tokens":21,"model":"deepseek-v4-pro"}, chat_usage={"call_count":1,"prompt_tokens":12,"completion_tokens":9} (1 completed call only), thread counters={"cumulative_input_tokens":12,"cumulative_output_tokens":9,"cumulative_cached_tokens":4} (12/9/4 - un-billed context never charged), header="21 / 1m↑ 12  ↓ 94$0.00" | 7.0s
PASS | #147 | Retrying an assistant that has no parent (root message, e.g. after deleting the root user message) creates the retry as a CHILD of the original instead of a sibling | retried root assistant: new response parent_id=null (NULL - proper root sibling, not a child of a1) sibling group count=2 | 8.2s
PASS | #150 | "Save as Branch" on a USER message keeps the ORIGINAL message's token attribution forever (the branch copy is never re-backfilled, so its token popover is stale/wrong) | branch-copied user message keeps token_count=0 (re-backfilled by the branch's own API call) and its popover shows " Token UsageInput: 0 tokens(contribution to context)" - the stale source attribution is replaced | 6.2s
PASS | #159 | Switching threads while a request is streaming persists the response into the WRONG thread (_persistStreamResponse reads activeThreadId at completion time, not the thread that sent the request) | sent in thread A, switched to thread B mid-stream: assistant rows inA=1 inB=0 - the "Hello from the mock LLM" response landed in thread A (the sender - captured at send time); A=["user:question for A","user:question for A","assistant:Hello from the mock LLM. This "] B=["user:question for B"] | 6.2s
PASS | #171 | Cancelling a stream AFTER switching threads writes the partial response into the WRONG thread (_handleStreamCancelled reads the current activeThreadId, same root cause as #159) | sent in A, switched to B, cancelled mid-stream: partial assistant rows inA=1 inB=0 (content="") - _handleStreamCancelled used the captured sending thread (A) | 6.1s
PASS | #172 | Hard-deleting (deleteThreadForever/emptyTrash) the streaming thread mid-stream silently DROPS the completed response - no dangling row (activeThreadId is cleared) but the billed response is never persisted anywhere | hard-delete mid-stream: thread A removed while streaming; on completion the captured thread id persists the response (dangling rows=1, any assistant row=1, chat_usage rows=1 (activeAtStart=true activeAtHardDelete=true) - the billed call leaves a trace and is usage-tracked | 9.0s
PASS | #178 | SSE `data:` LINE split across poll boundaries silently loses the payload (the remainder arrives without the `data: ` prefix, so SSEParser ignores it) | split-line stream: idle=true streamState.active=false, assistant persisted="Hello from the mock LLM. SPLIT-LEFT-RIGHT" - the split data line was re-formed by the pending-line buffer and its payload survives in full | 6.3s
PASS | #195 | Switching threads while a request is streaming pushes the OLD thread's completed response into the CURRENT thread's in-memory message array - _persistStreamedMessage always targets the global chatMessages, so after thread A's stream finishes while thread B is visible, B's UI array (copy/export/thread map) contains A's assistant message even though the DB row is correct in A | sent in A, switched to B mid-stream; DB rows inA=1 inB=0 (correct), and the visible thread B chatMessages=["user:question for B"] has no A response and no streaming bubble - stream messages are scoped to the sending thread/path | 8.6s
PASS | #203 | Chat-mode command with "Stream Response" OFF still streams - processInitialRequest's pasteMode="chat" branch never propagates the command's stream flag (it only persists model/system/reasoning/temperature), while ChatWindow.ahk initializes requestParams["stream"]:=true and ChatRequestBuilder streams whenever that flag is set; a JSON-only API response is then read by the SSE parser and shown as a request failure | ChatWindow defaults stream=true (normal chat); processInitialRequest passes the command's stream flag through notifyTriggerLLM; OnTriggerLLM writes requestParams["stream"] from wParam; and sendRequestToLLM routes stream=false to sendNonStreamingRequest (plain JSON) - a chat-mode command with Stream Response OFF no longer sends SSE | 0.0s
PASS | #205 | Cancelling a retry of a ROOT assistant (no parent) inserts the partial as a CHILD of the original - _handleStreamCancelled honors pendingRetrySiblingGroup but never checks pendingRetryIsRoot (unlike _persistStreamResponse, bug #147), so the cancelled retry is attached under the original root instead of as a sibling with parent NULL | retried the root assistant and cancelled after reasoning started: the partial row has parent_id=null (NULL - sibling of the original root) - _handleStreamCancelled now honors pendingRetryIsRoot | 6.4s
PASS | #206 | Switching threads mid-stream writes the OLD request's API-log entry with the NEW thread's model/provider - _logStreamResponse reads the CURRENT requestParams (singleAPIModelName/providerName) at completion instead of the values captured at send time, so the log row disagrees with the request body it logged | sent in A (request body model=deepseek-v4-flash), switched to B mid-stream; the API-log entry for A's request says model=deepseek/deepseek-v4-flash, provider=deepseek - loggers use the request metadata captured at send time | 6.2s
PASS | #208 | Streaming bubble author label injects assistant/model names as raw HTML (XSS) - createStreamingBubble concatenates displayName into innerHTML without escaping, so an assistant named <img onerror=...> executes in the WebView | assistant name <img src="x" onerror=...> rendered inert in the streaming bubble (window.__xssPwned=0); author innerHTML="&lt;img src=\"x\" onerror=\"window.__xssPwned=1\"&gt;" textContent="<img src=\"x\" onerror=\"window.__xssPwned=1\">" - the author label is escaped like every other bubble | 5.9s
PASS | #211 | A failed retry leaks pendingRetrySiblingGroup into the next response - retryAction sets the pending keys and only the SUCCESS (_persistStreamResponse) and CANCEL (_handleStreamCancelled) paths delete them; a retry rejected before any stream (vision gate / buildRequest failure / _handleStreamError) leaves the stale group set, so the next normal send inserts its response with the retried message's sibling_group even though its parent is the new user message | failed retry (vision gate) cleared pendingRetrySiblingGroup: follow-up response (parent=0ff6d2ed-b91d-023b-b498-0000a2ee9500) was inserted with sibling_group=null (clean; the retried assistant keeps its own group "0ff6cfb1-0aac-091e-1941-000065aef0bc") - the next normal send is no longer mis-grouped | 7.8s
PASS | #214 | Switching branches mid-stream re-enables the composer (setChatButtonsEnabled(true) inside updateChatMessages) - the user can send a SECOND request while the first stream is still in flight, and the second send clobbers the shared requestParams stream state so the first (billed) response is never persisted or logged | branch switch mid-stream kept the composer disabled (inputDisabled=true isLoading=true btn=stop); the first stream completed and its response was persisted (firstResp=1), no second message was created (secondResp=0) | 9.0s
PASS | #215 | Switching to an unanswered thread mid-stream leaves the loading indicator stuck after the stream completes - initChatMode shows the dots for the visible thread's trailing user message (isLoading is still true), and onStreamDone (scoped away by bug #195 for a non-current thread) never calls hideLoadingIndicator | switched to unanswered thread B mid-stream: loading dots shown while streaming=true, after A's stream completed (streamState.active=false) the dots are hidden in B=true - completion of the non-current stream clears the stuck indicator | 8.5s
PASS | #216 | A failed retry restores the removed messages into WHATEVER thread is currently visible - restoreRetryMessagesOnError pushes _retryRemovedMessages (thread A's messages) into the global chatMessages array, so switching to thread B before the retry fails repaints A's assistant message into B's UI (the DB rows stay correct in A, the same class as bug #195 on the error path) | retried a1 in A, switched to B, then the retry failed: B's UI array is ["m-216-u1b:question for B"] (thread A's "first answer" is NOT in the visible thread B) while the DB row stays in ["t-retry-a-216"] - restoreRetryMessagesOnError is scoped to the retry thread | 7.3s
PASS | #218 | Switching THREADS mid-stream leaves a mismatched composer state - initChatMode unconditionally re-enables the input and send button (disabled=false) but never re-wires the button, so while the old stream is still active the input is editable, isLoading=false, and the button still shows Stop; pressing Enter sends a SECOND request that clobbers the first stream (same family as #214, but on the loadThread/initChatMode path) | switched to assistant-ended thread B mid-stream: composer stayed disabled (inputDisabled=true isLoading=true btn=stop); a forced Enter keydown posted ["{\"action\":\"cancelStream\",\"reqId\":\"r8\"}"] (cancel, never chatSend) - the mismatch cannot fire a second send | 5.8s
PASS | #219 | Mid-stream SSE error event (`data: {"error": ...}`) crashes SSEParser.ParseLine - parsed["choices"] throws on a Map without a "choices" key, so the partial streamed response is never persisted, the user sees an internal Key "choices" error, and streamState.active stays true: every subsequent Send is swallowed as cancelStream (the composer is wedged until reload) | banner="upstream exploded (mock)×" streamState.active=false isLoading=false chatMessages=2 assistantRowsInDb=1 partialRowsInDb=1 - provider error surfaced, partial kept, composer un-wedged, follow-up Send posted chatSend | 5.7s

Summary: 21/21 scenarios PASS
Real profile restored: yes
Report sync: OK
