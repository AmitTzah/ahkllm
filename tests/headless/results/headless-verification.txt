# 2026-08-12T11:57:01.433Z | run of 14 scenario(s)
PASS | #194 | Overwrite-editing an assistant message leaves the thread's CUMULATIVE output tokens stale - MessageRepo.Edit re-estimates the message token_count (bug #181) and recomputes active_path_tokens, but never calls _RecomputeCumulativeCounters, so the header ledger disagrees with the per-message popover until the next API call | assistant overwrite-edit refreshed token_count=100 and chat_threads.cumulative_output_tokens followed to 100 (was 9 before the edit) - header ledger and per-message token count agree | 0.1s
PASS | #195 | Switching threads while a request is streaming pushes the OLD thread's completed response into the CURRENT thread's in-memory message array - _persistStreamedMessage always targets the global chatMessages, so after thread A's stream finishes while thread B is visible, B's UI array (copy/export/thread map) contains A's assistant message even though the DB row is correct in A | sent in A, switched to B mid-stream; DB rows inA=1 inB=0 (correct), and the visible thread B chatMessages=["user:question for B"] has no A response and no streaming bubble - stream messages are scoped to the sending thread/path | 8.9s
PASS | #196 | Fresh-profile default assistant loses isDefault - SettingsDefaults._DefaultsAssistants builds the defaults snapshot WITHOUT isDefault (DefaultSettings marks Natural Conversationalist isDefault:true), so with no settings.json the applied assistants have no default and _applyNewChatDefault falls through to the app default model | fresh defaults snapshot has 2 assistant(s), isDefault flags in snapshot=1, applied assistant globals with isDefault=1 - DefaultSettings marks Natural Conversationalist isDefault:true and the defaults builder preserves it, so App Default starts with the marked assistant | 0.1s
PASS | #197 | Switching BRANCHES within the SAME thread mid-stream mis-parents the completed response - _persistStreamResponse reads ChatDB.Msg_GetActivePath(streamThreadId) at completion time instead of capturing the request's last user message, so after branch A sends and the user navigates to branch B, the response becomes a child of branch B's leaf instead of branch A's follow-up user message | branch A sent "follow-up on A" (user id 09d48484-920b-cec0-aa77-00004937ea41), switched to branch B mid-stream; the completed response was inserted with parent_id=09d48484-920b-cec0-aa77-00004937ea41 - _persistStreamResponse uses the parent captured at send time, so the response stays on the branch that sent it | 9.3s
PASS | #198 | PDF/office attachments with a generic MIME type are misclassified as text_file - getAttachmentTypeFromMime only falls back to the extension for odt/odp/ods/rtf/epub, so a .pdf/.docx/.pptx/.xlsx with application/octet-stream is treated as plain text (no PDF/office extraction, sent as garbled text context) | File "report.pdf" with MIME application/octet-stream was classified as "pdf" - getAttachmentTypeFromMime now falls back to the extension for pdf/docx/pptx/xlsx, so the PDF is attached as a PDF and extracted | 3.9s
PASS | #199 | With zero configured providers, a chat send crashes inside the API-key error handler - ProviderResolver.Resolve returns providerKey="" and ChatRequestBuilder._ShowApiKeyError indexes providers[""] unguarded (a missing Map key THROWS in AHK v2), so the friendly "No API key configured" error never reaches the UI | providers={}, providerMap={}: ProviderResolver.Resolve returned providerKey="" and _ShowApiKeyError completed without throwing - the friendly key error reaches the UI | 0.1s
PASS | #200 | Usage dashboard command rows with an EMPTY provider but a provider-prefixed model are unfilterable - renderMainChart keys command rows by c.provider||"" (unlike chat rows, which fall back to extractProvider(c.model)), while populateFilters only adds the "Unknown (blank)" sentinel when BOTH provider and extractProvider(model) are empty; the "" command series renders with no selectable filter | command row provider="" model="deepseek/gpt-5": provider-mode chart labels=["deepseek"] (charts under "deepseek", matching the filter options) - no unfilterable "" series remains | 0.0s
PASS | #201 | Assistant Reasoning dropdown ignores short-form base model ids - ReasoningLevels.levelsForModel only checks the exact models[baseModel] key, so an assistant whose baseModel is "gpt-5-mini" (instead of "openai/gpt-5-mini") falls back to the generic list and offers "None (Disabled)"/"Minimal"/"Medium" options the model does not support (the same short-form family as bugs #43/#51, on the assistant settings path) | assistant baseModel="gpt-5-mini" (map key openai/gpt-5-mini with thinkingLevelMap {low, high}): ReasoningLevels.buildOptionsHtml returned ["","low","high"] - the dropdown offers only the levels the model actually supports | 0.0s
PASS | #202 | Forking a thread that contains a "Save as Branch" local copy re-counts the copy as a REAL API call - TreeRepo fork INSERTs do not copy is_local_copy, so _RecomputeCumulativeCounters charges the copied tokens/cost a second time and the fork header disagrees with the source thread and the dashboard | source thread (1 real API call 12/9/4 + 1 local copy): cumulative=12/9/4; fork has 1 is_local_copy row(s) and cumulative=12/9/4 - the fork preserves the local-copy flag, so its header agrees with the source and the dashboard | 0.2s
PASS | #203 | Chat-mode command with "Stream Response" OFF still streams - processInitialRequest's pasteMode="chat" branch never propagates the command's stream flag (it only persists model/system/reasoning/temperature), while ChatWindow.ahk initializes requestParams["stream"]:=true and ChatRequestBuilder streams whenever that flag is set; a JSON-only API response is then read by the SSE parser and shown as a request failure | ChatWindow defaults stream=true (normal chat); processInitialRequest passes the command's stream flag through notifyTriggerLLM; OnTriggerLLM writes requestParams["stream"] from wParam; and sendRequestToLLM routes stream=false to sendNonStreamingRequest (plain JSON) - a chat-mode command with Stream Response OFF no longer sends SSE | 0.0s
PASS | #204 | A stalled stream never times out - CurlBuilder.BuildStream has --connect-timeout 30 but NO --max-time, so an API that accepts the connection and then sends nothing leaves streamState.active/isLoading true and the Stop/input UI stuck forever | CurlBuilder.BuildStream now includes --max-time 120 alongside --connect-timeout 30 - a stalled upstream cannot hang the chat UI forever | 0.0s
PASS | #205 | Cancelling a retry of a ROOT assistant (no parent) inserts the partial as a CHILD of the original - _handleStreamCancelled honors pendingRetrySiblingGroup but never checks pendingRetryIsRoot (unlike _persistStreamResponse, bug #147), so the cancelled retry is attached under the original root instead of as a sibling with parent NULL | retried the root assistant and cancelled after reasoning started: the partial row has parent_id=null (NULL - sibling of the original root) - _handleStreamCancelled now honors pendingRetryIsRoot | 6.7s
PASS | #206 | Switching threads mid-stream writes the OLD request's API-log entry with the NEW thread's model/provider - _logStreamResponse reads the CURRENT requestParams (singleAPIModelName/providerName) at completion instead of the values captured at send time, so the log row disagrees with the request body it logged | sent in A (request body model=deepseek-v4-flash), switched to B mid-stream; the API-log entry for A's request says model=deepseek/deepseek-v4-flash, provider=deepseek - loggers use the request metadata captured at send time | 6.3s
PASS | #207 | Thread A's stream completion repaints thread B's header token bar with A's stats - _handleStreamComplete calls postThreadStats(streamThreadId) and the WebView's updateTokenUsage renders whatever arrives, so after switching to B mid-stream B shows A's Context/Cumulative numbers even though B's DB has only a 5-token user message | after A completes while B is visible: B DB stats={"cumulative_input_tokens":0,"cumulative_output_tokens":0} (context 5, no API calls); the header shows B's stats "5 / 1m↑ 0 ↓ 00$0.00" - updateTokenUsage ignores stats whose threadId does not match the active thread | 6.1s

Summary: 14/14 scenarios PASS
Real profile restored: yes
Report sync: OK
