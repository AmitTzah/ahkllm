# 2026-08-13T12:07:28.878Z | run of 2 scenario(s)
PASS | #223 | Two quick chat-mode commands leak the FIRST request's temp files - deleteTempFiles() only ever reads requestParams' CURRENT paths (the second request's), so the clobbered first request's cURL command file (which embeds the Authorization: Bearer API key) plus its request/output/error files stay in %TEMP% forever: the bug #110 cleanup guarantee is broken by the bug #221 race | after the two-command race: 3 new leftover ChatWindow_* temp files, 1 cURL command(s) still containing "Authorization: Bearer" - deleteTempFiles only removed the second request's paths, so the first request's files (with the API key) stay in %TEMP% | 13.9s
PASS | #224 | User message bubbles also collapse SINGLE-newline paragraph breaks - _prepUserContent only converts 3+ newlines to <br>, so a multi-paragraph message (or a pasted selection) whose paragraphs are separated by single newlines renders as ONE block in the user's own bubble (the mirror of bug #222 on the user side) | user sent 3 paragraphs separated by single newlines; user bubble pCount=1 innerText="First paragraph of the selection. Second paragraph of the selection. Third paragraph of the selection." - the multi-paragraph message displays as ONE block | 4.3s

Summary: 2/2 scenarios PASS
Real profile restored: yes
Report sync: OK
