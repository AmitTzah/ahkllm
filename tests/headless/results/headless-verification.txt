# 2026-08-12T10:44:39.540Z | run of 4 scenario(s)
PASS | #194 | Overwrite-editing an assistant message leaves the thread's CUMULATIVE output tokens stale - MessageRepo.Edit re-estimates the message token_count (bug #181) and recomputes active_path_tokens, but never calls _RecomputeCumulativeCounters, so the header ledger disagrees with the per-message popover until the next API call | assistant overwrite-edit refreshed token_count=100 but chat_threads.cumulative_output_tokens stayed 9 (was 9 before the edit) - the header ledger and the per-message token count disagree until the next API call | 0.2s
PASS | #195 | Switching threads while a request is streaming pushes the OLD thread's completed response into the CURRENT thread's in-memory message array - _persistStreamedMessage always targets the global chatMessages, so after thread A's stream finishes while thread B is visible, B's UI array (copy/export/thread map) contains A's assistant message even though the DB row is correct in A | sent in A, switched to B mid-stream; DB rows inA=1 inB=0 (correct), but the visible thread B chatMessages=["user:question for B","assistant:Hello from the mock LLM. This is the streamed answer."] contains A's assistant response - _persistStreamedMessage targets the global chatMessages instead of the sending thread's UI state | 6.2s
PASS | #196 | Fresh-profile default assistant loses isDefault - SettingsDefaults._DefaultsAssistants builds the defaults snapshot WITHOUT isDefault (DefaultSettings marks Natural Conversationalist isDefault:true), so with no settings.json the applied assistants have no default and _applyNewChatDefault falls through to the app default model | fresh defaults snapshot has 2 assistant(s), isDefault flags in snapshot=0, applied assistant globals with isDefault=0 - DefaultSettings marks Natural Conversationalist isDefault:true, but the defaults builder drops it, so App Default starts with the model instead | 0.1s
PASS | #197 | Switching BRANCHES within the SAME thread mid-stream mis-parents the completed response - _persistStreamResponse reads ChatDB.Msg_GetActivePath(streamThreadId) at completion time instead of capturing the request's last user message, so after branch A sends and the user navigates to branch B, the response becomes a child of branch B's leaf instead of branch A's follow-up user message | branch A sent "follow-up on A" (user id 09923924-cce2-7784-03b0-0000dcba5800), switched to branch B mid-stream; the completed response was inserted with parent_id=m-197-a2b (should be 09923924-cce2-7784-03b0-0000dcba5800) - _persistStreamResponse read the CURRENT active path instead of the request's own parent | 9.5s

Summary: 4/4 scenarios PASS
Real profile restored: yes
Report sync: OK
